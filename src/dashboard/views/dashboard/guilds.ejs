<!DOCTYPE html>
<html lang="en">
    <head>
      <%- include('../components/header.ejs') %>
      <link rel="stylesheet" href="/css/sidebar.css">
   <style>
   h3.title {
   font-color: dark;
   font-size: 30px;
   }
   .switch {
  position: relative;
  display: inline-block;
  width: 60px;
  height: 34px;
}

.switch input { 
  opacity: 0;
  width: 0;
  height: 0;
}

.slider {
  position: absolute;
  cursor: pointer;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #ccc;
  -webkit-transition: .4s;
  transition: .4s;
}

.slider:before {
  position: absolute;
  content: "";
  height: 26px;
  width: 26px;
  left: 4px;
  bottom: 4px;
  background-color: white;
  -webkit-transition: .4s;
  transition: .4s;
}

input:checked + .slider {
  background-color: #2196F3;
}

input:focus + .slider {
  box-shadow: 0 0 1px #2196F3;
}

input:checked + .slider:before {
  -webkit-transform: translateX(26px);
  -ms-transform: translateX(26px);
  transform: translateX(26px);
}

/* Rounded sliders */
 .slider.round {
  border-radius: 34px;
}

.slider.round:before {
  border-radius: 50%;
} 
label.textlabel {
font-size: 20px;
text-align: center;
} 

#checkbox1:checked ~ #msgch {
    display: block;
}
   </style>
    </head>
  <body>
    

   <!-- <div class="d-flex" id="wrapper"> -->
      
    <!--  <div class="extension"> 
       -->
       <!-- <header class="text-center py-4 s="text-center py-4 px-2" style="background-color: #2c2e33;"> -->
          
<!--           <div style="text-align: end;">
            <svg width="3em" height="3em" viewBox="0 0 16 16" class="bi bi-x" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
              <path fill-rule="evenodd" d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708z"/>
            </svg>
          </div> -->
          
      
        <!--  <h4 class="pt-2"><guild.name></h4>
          </header>
      </div> -->
      
      <div id="page-content-wrapper" style="width: 100%;">
  
        <%- include ('../components/navbar.ejs') %>
  
       <div class="container-fluid text-center">
          <h1 class="mt-4"><%=guild.name%></h1>
         <h2><%=guild.memberCount%> Membros</h2>
          <img src="https://cdn<%=guild.id%>" alt="">
       <form
  id="save"
  method="POST"
  autocomplete="off"
  action="/dashboard/guilds/<%=guild.id%>/post/prefixo"
  enctype="application/x-www-form-urlencoded"
>
  <!-- form pra enviar o prefixo -->
  <label for="botprefix">Prefixo:</label><br />

  <input
    type="text"
    id="botprefix"
    class="form-control"
    name="botprefix"
    placeholder="Prefixo atual: <%=fireprefix%>"
    value="<%=fireprefix%>"
  />
  <br>
  <button type="submit" class="btn btn-primary">Guardar</button>
</form>
  <br /><br />
    
 <form
  id="save"
  method="POST"
  autocomplete="off"
  action="/dashboard/guilds/<%=guild.id%>/post/execlog"
  enctype="application/x-www-form-urlencoded"
>
   <br>
  <label for="execlog">Avisar quando executar um comando desconhecido: (Atual: <%=cmdstats%> )</label>
 
                <select class="form-control" id="execlog" name="execlog" required="">
                  <option>Não</option>
                  <option>Sim</option>
                </select>
   <br>
   <button type="submit" class="btn btn-primary">Guardar</button>

</form>
<br>
 <h3 class="title">Entradas/Saídas</h3>
    <br>
    <form
  id="save"
  method="POST"
  autocomplete="off"
  action="/dashboard/guilds/<%=guild.id%>/post/entrada"
  enctype="application/x-www-form-urlencoded">
<!---<label class="switch"> --->
   
 
  <!---<span class="slider round"></span>
</label> --->
  <label class="textlabel" for="welch">Canal de Entradas</label><br>
<select class="form-control" name="punishchannel" required="">
                 <option value="null" selected disabled>Selecione um canal (atual: <%=welch%></option>
                  <% guild.channels.filter((ch) => ch.type === 0).forEach((ch) => { %>
                  <option value="<%= ch.id%>">#<%= ch.name%></option>
                  <% }); %>
                </select>
 <br><br>
 <h1> Em breve .....</h1>
</form>
        </div>
      </div>
        
      </div>
  
    </div>
  </body>
</html>